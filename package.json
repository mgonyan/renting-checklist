{
  "name": "renting-checklist",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "node ./bin/www",
    "test": "jest",
    "heroku-postbuild": "cd client && npm install --only=dev && npm install && npm run build"
  },
  "dependencies": {
    "body-parser": "^1.17.2",
    "cookie-parser": "^1.4.3",
    "debug": "^2.6.8",
    "express": "^4.15.4",
    "jade": "^1.11.0",
    "morgan": "^1.8.2",
    "serve-favicon": "^2.4.3"
  },
  "devDependencies": {
    "eslint": "^4.4.1",
    "jest": "^20.0.4"
  },
  "jest": {
    "modulePathIgnorePatterns": [
      "<rootDir>/client"
    ]
  }
}
